export async function centralFetch(path:string, init?:RequestInit){const b=process.env.CENTRAL_BASE_URL;const s=process.env.CENTRAL_AUTH_SCHEME||"Bearer";const t=process.env.CENTRAL_TOKEN;if(!b) throw new Error("CENTRAL_BASE_URL not set");const h=new Headers(init?.headers||{});h.set("Authorization", s==="Basic"?`Basic ${t}`:`Bearer ${t}`);if(!h.has("Content-Type")) h.set("Content-Type","application/json");const r=await fetch(`${b}${path}`,{...init,headers:h,cache:"no-store"});return r;}