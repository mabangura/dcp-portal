'use client';import { useEffect, useState } from 'react';import Link from 'next/link';export default function ProjectSettings({ params }:{params:{id:string}}){const id=params.id;const key=`dcp_dashboard_url_${id}`;const [url,setUrl]=useState('');const [saved,setSaved]=useState(false);useEffect(()=>{setUrl(localStorage.getItem(key)||'');},[key]);function save(){localStorage.setItem(key,url);setSaved(true);setTimeout(()=>setSaved(false),1200);}return(<div className="space-y-4"><div className="flex items-center justify-between"><h1 className="text-xl font-semibold">Project {id} — Settings</h1><Link href={`/projects/${id}`} className="btn">Back</Link></div><div className="card space-y-3"><div className="text-sm text-slate-500">Embedded Dashboard URL</div><input className="w-full border p-2 rounded" placeholder="https://bi.example.com/dashboard/xyz" value={url} onChange={e=>setUrl(e.target.value)} /><button className="btn btn-primary" onClick={save}>Save</button>{saved&&<div className="text-xs text-emerald-600">Saved</div>}</div>{url&&<div className="card"><div className="text-sm text-slate-500 mb-2">Preview</div><iframe src={url} className="w-full h-[480px] rounded-lg border" /></div>}</div>);}