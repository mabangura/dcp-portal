import Link from 'next/link';async function getProjects(){const r=await fetch('/api/projects',{cache:'no-store'});return r.json();}export default async function ProjectsPage(){const projects=await getProjects();return(<div><h1 className="text-xl font-semibold mb-4">Projects</h1><div className="grid gap-4">{projects?.map((p:any)=>(<div key={p.id} className="card flex items-center justify-between"><div><div className="font-medium">{p.name}</div><div className="text-xs text-slate-500">ID: {p.id}</div></div><div className="flex gap-2"><Link href={`/projects/${p.id}`} className="btn">View</Link></div></div>))}</div></div>);}